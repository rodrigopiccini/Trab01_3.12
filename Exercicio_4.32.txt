#include <iostream>

using namespace std

int main()

{
	double a,b,c; // variáveis valores do triangulo


	cout << "Digite um valor para o primeiro lado do triângulo: " << endl;
	cin >> a;

	cout << "Digite um valor para o segundo lado do triângulo: " << endl;
	cin >> b;

	cout << "Digite um valor para o primeiro lado do triângulo: " << endl;
	cin >> c;


	{
	if (a < b+c) && (b < a+c) && (c < a+b)
	cout << "Estes valores formam um triangulo." << endl;
	}

	{
	if (a==b) e (b==c)
	cout << "Triangulo equilatero." << endl;
	}

    {
	if (a==b) || (b==c) || (c==a)
    cout << "Triângulo Isoceles." << endl;
	}

	{
	if (a==0) || (b==0) || (c==0)
	cout << "Nenum valor pode ser zero." << endl;
	}

	else
	cout << "Triângulo escaleno." << endl;

	return 0;

}
